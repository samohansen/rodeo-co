import type { NextPage } from "next";

const IndexPage: NextPage = () => {
  return (
    <div>
      hey folks it's me the home-page-home-page (not the rodeos dashboard)
    </div>
  );
};

export default IndexPage;